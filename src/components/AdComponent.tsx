// AdComponent — disabled
// RepayWise does not serve third-party ads. No ad scripts are loaded.
export function AdComponent(_props: { slot?: string; className?: string } = {}) {
  return null;
}
export default AdComponent;
